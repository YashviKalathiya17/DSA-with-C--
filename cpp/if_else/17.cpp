#include <iostream>
using namespace std;

int main()
{

    // calculate the roots of a quadratic quation using if-else conditions.

    // int a, b, c;
    // cout << "Enter coefficients a, b, and c of the quadratic equation ax^2 + bx + c = 0: ";
    // cin >> a >> b >> c;

    // double discriminant = b * b - 4 * a * c;
    // double root1, root2;

    // if (discriminant > 0)
    // {
    //     root1 = (-b + sqrt(discriminant)) / (2 * a);
    //     root2 = (-b - sqrt(discriminant)) / (2 * a);
    //     cout << "Two distinct real roots are: " << root1 << " and " << root2 << endl;
    // }
    // else if (discriminant == 0)
    // {
    //     root1 = root2 = -b / (2 * a);
    //     cout << "Two equal real roots are: " << root1 << endl;
    // }
    // else
    // {
    //     cout << "The equation has no real roots." << endl;
    // }

    return 0;
}